{
  "name": "track-shared-logsdb-mode",
  "component_template": {
      "template": {
        "settings": {
          {% if index_mode %}
          "index": {
              "mode": {{ index_mode | tojson }}
          }
          {% endif %}
        }
      }
  }
}
